# spring-crud
Basic spring CRUD application with bootstrap

Before running this project create a table name authors in oracle database with the following specs(
  id number(5)
  name varchar2(200)
  email varchar2(200
)
